import { Centreinvistissement } from './centreinvistissement';

describe('Centreinvistissement', () => {
  it('should create an instance', () => {
    expect(new Centreinvistissement()).toBeTruthy();
  });
});
